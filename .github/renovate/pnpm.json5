{
  $schema: 'https://docs.renovatebot.com/renovate-schema.json',

  // Run pnpm dedupe after every dependency update
  postUpdateOptions: ['pnpmDedupe'],

  lockFileMaintenance: {
    enabled: true,
    schedule: ['before 7am on the first day of the month'],
  },
}